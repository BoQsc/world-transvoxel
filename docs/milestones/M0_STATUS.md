# M0 Status: Addon and Official Upstream Baseline

Status: complete

Completed: 2026-06-20

Validated platform: Windows x86-64

## Delivered

- Self-contained `addons/world_transvoxel/` boundary.
- Official `Transvoxel.cpp` isolated under
  `thirdparty/transvoxel_mit/`.
- Official source byte hash and upstream revision validation.
- MIT notice and mixed 0BSD/MIT scope documentation.
- Pinned godot-cpp submodule at
  `e83fd0904c13356ed1d4c3d09f8bb9132bdc6b77`.
- Pinned Zig 0.16.0 bootstrap with archive hash validation.
- SCons binding generation with Zig compilation, archiving, and linking.
- Validated PE timestamp normalization for deterministic DLL bytes.
- Minimal godot-cpp build profile.
- Debug and release GDExtension binaries.
- Project-owned `WtMeshingBackend` interface.
- Minimal official MIT adapter with private table inclusion.
- Registered `WorldTransvoxelTerrain` GDExtension class.
- Repository, provenance, license, binary, and Godot load validation.

## Official source integrity

```text
Upstream revision
51a494f03c5b024cd153b596bcc7152eb3cc93a6

Transvoxel.cpp SHA-256
83a5511346b54c42e4e66dec916d3971c92f4fbda1c7878cbad5901a820dcab4
```

The official source file remains byte-for-byte equal to the pinned Windows
checkout and is excluded from line-ending normalization.

## Build matrix

```text
Compiler/linker     Zig 0.16.0
Binding generator   godot-cpp/SCons
godot-cpp           godot-4.5-stable
Architecture        Windows x86-64
Configurations      template_debug, template_release
Build profile       Node3D, OS and required binding dependencies
```

The generated binding profile contains 12 engine classes rather than the full
971-class API generated by an unrestricted build.

Zig's Windows linker output differed only in the four-byte PE COFF timestamp.
The build validates the PE structure and zeros that non-functional metadata
field after linking. Repeated relinks must then produce identical DLL hashes.

M0 normalized binary hashes:

```text
template_debug
96bdaa97f519923e38ae0f0222af5fca0c7663011e9ac77e33446eadd3a019f2

template_release
b2f1a60d0cde3eb6d5d184f8edd5bf8e7f147dd8463c5afa0147ed3f37ed4ed7
```

## Runtime matrix

```text
Godot 4.6.3 stable   debug PASS, release PASS
Godot 4.7 stable     debug PASS, release PASS
```

Each test:

1. starts a real headless Godot editor import/load;
2. verifies the GDExtension;
3. registers `WorldTransvoxelTerrain`;
4. instantiates the native class;
5. verifies addon version and milestone;
6. verifies official backend availability, ID, MIT license, and upstream
   revision.

## Scope confirmation

M0 contains no regular-cell meshing implementation, transition-cell meshing
implementation, chunk ownership, streaming, render integration, collision,
editing, baking, storage, or compute acceleration.

M1 through M4 subsequently completed. The current active milestone is M5:
streaming production baseline.
