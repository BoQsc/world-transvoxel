# Predictive interaction shell checkpoint

Authority base: `208013cee5abf747263f5a04e6f202563177b914`

The production integration expands player support and tool-focus paths into a
bounded one-chunk LOD0 shell. Exact path centers enter the 64-key foreground
lease first; nearest Manhattan halo layers then consume remaining capacity.
This keeps a complete aimed path prioritized while warming the neighboring
pages required by atomic regional publication.

The deterministic cached-approach route warms all 27 keys around its target
through the reserved interaction storage lane and rejects the run on any warm
admission failure. Vulkan readiness improved from approximately 101 ms to
84.342 ms traced and 83.299 ms untraced. D3D12 measured 83.839 ms traced and
83.349 ms untraced. All four runs pass the enforced 100 ms cached-approach
contract.

The native addon and source trees are unchanged from the preceding fully
qualified checkpoint. Its seven regressions in debug and release, collision
continuity, rapid-edit publication, deterministic hashes, and binary artifact
therefore remain identical.
